namespace BTPSecure.Shared.Enums;

// Détermine la couleur de la petite notification affichée à la connexion
public enum Enum_SeveriteNotification
{
    Info = 0,
    Succes = 1,
    Avertissement = 2
}
